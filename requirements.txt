torch>=2.4
transformers>=5.0
peft>=0.14
accelerate>=1.0
datasets>=3.0
safetensors>=0.4
numpy>=1.26
requests>=2.31
PyYAML>=6.0
json5>=0.9
tiktoken>=0.7
openai>=1.0
wandb>=0.17
pytest>=8.0

# 可选组件（按阶段单独安装）
# trl>=0.20          # DPO/GRPO 训练
# liger-kernel>=0.5  # fused sparse CE
# faiss-cpu>=1.8     # CPU retriever 服务
# flash-attn>=2.7    # 兼容时启用 FlashAttention2
# verl               # 在线 GRPO；按上游文档安装
