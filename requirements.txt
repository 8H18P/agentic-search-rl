torch==2.11.0
transformers==5.8.0
peft==0.18.1
accelerate==1.11.0
datasets==4.0.0
safetensors>=0.4
numpy>=1.26
requests>=2.31
PyYAML>=6.0
json5>=0.9
tiktoken>=0.7
openai>=1.0
wandb>=0.17
pytest>=8.0

# 可选组件（按阶段单独安装）
# GRPO 专用环境固定使用 trl==1.12.0；见 requirements-grpo-lock.txt。
# liger-kernel       # 仅在明确启用 fused sparse CE 时安装并记录版本
# faiss-cpu          # retriever 服务环境单独冻结
# flash-attn>=2.7    # 兼容时启用 FlashAttention2
